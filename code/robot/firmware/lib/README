
本目录存放固件工程的私有功能库。PlatformIO 将这些模块编译为静态库并链接到固件程序。

当前固件库按功能分别存放在以下目录：

- `chassis/`：三轮底盘运动学、PID 和反馈安全状态。
- `imu/`：IMU 初始化、采样和姿态数据处理。
- `lidar/`：MS200 雷达帧解析和扫描数据处理。
- `motor/`：电机、PWM 和编码器接口。

PlatformIO Library Dependency Finder 根据固件源码中的头文件引用解析模块依赖。
